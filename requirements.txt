# Core deep learning
torch>=2.0
torchvision>=0.15

# Image / array processing
opencv-python>=4.8
numpy>=1.24
pillow>=10.0
albumentations>=1.3

# OCR metrics / text distance
editdistance>=0.6
python-Levenshtein>=0.21
jiwer>=3.0

# Config / utils
pyyaml>=6.0
tqdm>=4.65
matplotlib>=3.7
scikit-learn>=1.3

# --- Google Colab notes ---
# Colab already ships torch, torchvision, numpy, pillow, opencv-python, matplotlib
# and scikit-learn. There you typically only need:
#   pip install albumentations editdistance jiwer pyyaml
# Select a GPU runtime (Runtime > Change runtime type > T4 GPU) before training.